---
uid: web-forms/overview/older-versions-getting-started/master-pages/urls-in-master-pages-vb
title: 마스터 페이지 (VB)의 Url | Microsoft Docs
author: rick-anderson
description: 마스터 페이지의 Url이 마스터 페이지 파일의 콘텐츠 페이지 이외의 다른 상대 디렉터리에 있는 것으로 인해 중단 될 수 있습니다 하는 방법을 다룹니다. 기준 주소 다시 지정 하는 방법을 살펴봅니다.
ms.author: riande
ms.date: 06/10/2008
ms.assetid: 43d1e83c-0092-4dcf-977c-e709c4dce7c3
msc.legacyurl: /web-forms/overview/older-versions-getting-started/master-pages/urls-in-master-pages-vb
msc.type: authoredcontent
ms.openlocfilehash: 881debeeaa98a7f2be7ccadb501c019e698b22f2
ms.sourcegitcommit: 45ac74e400f9f2b7dbded66297730f6f14a4eb25
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 08/16/2018
ms.locfileid: "41835688"
---
<a name="urls-in-master-pages-vb"></a><span data-ttu-id="76cfd-104">마스터 페이지 (VB)의 Url</span><span class="sxs-lookup"><span data-stu-id="76cfd-104">URLs in Master Pages (VB)</span></span>
====================
<span data-ttu-id="76cfd-105">[Scott Mitchell](https://twitter.com/ScottOnWriting)</span><span class="sxs-lookup"><span data-stu-id="76cfd-105">by [Scott Mitchell](https://twitter.com/ScottOnWriting)</span></span>

<span data-ttu-id="76cfd-106">[코드를 다운로드](http://download.microsoft.com/download/e/e/f/eef369f5-743a-4a52-908f-b6532c4ce0a4/ASPNET_MasterPages_Tutorial_04_VB.zip) 또는 [PDF 다운로드](http://download.microsoft.com/download/8/f/6/8f6349e4-6554-405a-bcd7-9b094ba5089a/ASPNET_MasterPages_Tutorial_04_VB.pdf)</span><span class="sxs-lookup"><span data-stu-id="76cfd-106">[Download Code](http://download.microsoft.com/download/e/e/f/eef369f5-743a-4a52-908f-b6532c4ce0a4/ASPNET_MasterPages_Tutorial_04_VB.zip) or [Download PDF](http://download.microsoft.com/download/8/f/6/8f6349e4-6554-405a-bcd7-9b094ba5089a/ASPNET_MasterPages_Tutorial_04_VB.pdf)</span></span>

> <span data-ttu-id="76cfd-107">마스터 페이지의 Url이 마스터 페이지 파일의 콘텐츠 페이지 이외의 다른 상대 디렉터리에 있는 것으로 인해 중단 될 수 있습니다 하는 방법을 다룹니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-107">Addresses how URLs in the master page can break due to the master page file being in a different relative directory than the content page.</span></span> <span data-ttu-id="76cfd-108">통해 Url 기준 주소 다시 지정 살펴봅니다 ~ 선언적 구문 및 ResolveUrl 및 ResolveClientUrl를 프로그래밍 방식으로 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-108">Looks at rebasing URLs via ~ in the declarative syntax and using ResolveUrl and ResolveClientUrl programmatically.</span></span> <span data-ttu-id="76cfd-109">(또한 살펴보고</span><span class="sxs-lookup"><span data-stu-id="76cfd-109">(Also look at</span></span>


## <a name="introduction"></a><span data-ttu-id="76cfd-110">소개</span><span class="sxs-lookup"><span data-stu-id="76cfd-110">Introduction</span></span>

<span data-ttu-id="76cfd-111">모든 예제에서 살펴본 지금 동일한 폴더 (웹 사이트의 루트 폴더)에 마스터 및 콘텐츠 페이지 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-111">In all the examples we've seen thus far, the master and content pages have been located in the same folder (the website's root folder).</span></span> <span data-ttu-id="76cfd-112">하지만 동일한 폴더에 마스터 및 콘텐츠 페이지 해야 이유 이유가 없습니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-112">But there's no reason why the master and content pages must be in the same folder.</span></span> <span data-ttu-id="76cfd-113">확실히 하위 폴더의 콘텐츠 페이지를 만들 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-113">You can certainly create content pages in subfolders.</span></span> <span data-ttu-id="76cfd-114">마찬가지로, 만들 수는 `~/MasterPages/` 폴더 사이트의 마스터 페이지를 배치 하는 위치입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-114">Similarly, you might create a `~/MasterPages/` folder where you place your site's master pages.</span></span>

<span data-ttu-id="76cfd-115">서로 다른 폴더에 마스터 및 콘텐츠 페이지를 배치 하는 한 가지 잠재적인 문제가 손상 된 Url에 포함 됩니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-115">One potential issue with placing master and content pages in different folders involves broken URLs.</span></span> <span data-ttu-id="76cfd-116">하이퍼링크, 이미지 또는 다른 요소에서 상대 Url을 포함 하는 마스터 페이지, 링크를 다른 폴더에 있는 콘텐츠 페이지에 대 한 유효한 됩니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-116">If the master page contains relative URLs in hyperlinks, images, or other elements, the link will be invalid for content pages that reside in a different folder.</span></span> <span data-ttu-id="76cfd-117">이 자습서에서는 소스를 대안 뿐만 아니라이 문제를 살펴봅니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-117">In this tutorial we examine the source of this problem as well as workarounds.</span></span>

## <a name="the-problem-with-relative-urls"></a><span data-ttu-id="76cfd-118">상대 Url 사용 하 여 문제</span><span class="sxs-lookup"><span data-stu-id="76cfd-118">The Problem with Relative URLs</span></span>

<span data-ttu-id="76cfd-119">웹 페이지의 URL 이라고는 *상대 URL* 토큰이 가리키는 리소스의 위치는 웹 사이트의 폴더 구조에서 웹 페이지의 위치를 기준으로 하는 경우.</span><span class="sxs-lookup"><span data-stu-id="76cfd-119">A URL on a web page is said to be a *relative URL* if the location of the resource it points to is relative to the web page's location in the website's folder structure.</span></span> <span data-ttu-id="76cfd-120">선행 슬래시로 시작 하지 않는 모든 URL (`/`) 또는 프로토콜 (같은 `http://`) 상대 이므로 URL이 포함 된 웹 페이지의 위치를 기반으로 브라우저에서 확인 됩니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-120">Any URL that does not start with a leading forward slash (`/`) or a protocol (such as `http://`) is relative because it is resolved by the browser based on the location of the web page that contains the URL.</span></span>

<span data-ttu-id="76cfd-121">예를 들어 웹 사이트에는 `~/Images/` 단일 이미지 파일, 폴더 `PoweredByASPNET.gif`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-121">For example, our website has an `~/Images/` folder with a single image file, `PoweredByASPNET.gif`.</span></span> <span data-ttu-id="76cfd-122">마스터 페이지 파일을 `Site.master` 에 `<img>` 요소에는 `footerContent` 다음 태그를 사용 하 여 지역:</span><span class="sxs-lookup"><span data-stu-id="76cfd-122">The master page file `Site.master` has an `<img>` element in the `footerContent` region with the following markup:</span></span>


[!code-html[Main](urls-in-master-pages-vb/samples/sample1.html)]

<span data-ttu-id="76cfd-123">합니다 `src` 특성 값을 `<img>` 요소 이므로 상대 URL로 시작 하지 않습니다 `/` 또는 `http://`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-123">The `src` attribute value in the `<img>` element is a relative URL because it does not start with `/` or `http://`.</span></span> <span data-ttu-id="76cfd-124">즉, 합니다 `src` 특성 값 표시 하도록 브라우저에 지시 합니다 `Images` 라는 파일에 대 한 하위 폴더 `PoweredByASPNET.gif`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-124">In short, the `src` attribute value tells the browser to look in the `Images` subfolder for a file named `PoweredByASPNET.gif`.</span></span>

<span data-ttu-id="76cfd-125">콘텐츠 페이지를 방문 하는 경우 위의 태그는 브라우저에 직접 전송 됩니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-125">When visiting a content page, the above markup is sent directly to the browser.</span></span> <span data-ttu-id="76cfd-126">방문을 내어 `About.aspx` 브라우저로 전송의 HTML 소스를 확인 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-126">Take a moment to visit `About.aspx` and view the HTML source sent to the browser.</span></span> <span data-ttu-id="76cfd-127">브라우저에 마스터 페이지의 정확히 동일한 태그를 보냈음을 찾을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-127">You will find that the exact same markup in the master page was sent to the browser.</span></span>


[!code-html[Main](urls-in-master-pages-vb/samples/sample2.html)]

<span data-ttu-id="76cfd-128">콘텐츠 페이지 루트 폴더에 있으면 (그대로 `About.aspx`) 있기 때문에 예상 대로 작동 하는 모든 항목을 `Images` 루트 폴더에 상대적인 하위 폴더입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-128">If the content page is in the root folder (as is `About.aspx`) everything works as expected because there is an `Images` subfolder relative to the root folder.</span></span> <span data-ttu-id="76cfd-129">그러나 콘텐츠 페이지 마스터 페이지와 다른 폴더에 있으면 작업 중단 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-129">However, things break down if the content page is in a different folder than the master page.</span></span> <span data-ttu-id="76cfd-130">예를 들어를 라는 하위 폴더를 만듭니다 `Admin`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-130">To illustrate this, create a subfolder named `Admin`.</span></span> <span data-ttu-id="76cfd-131">다음으로 명명 된 콘텐츠 페이지를 추가 `Default.aspx` 에 `Admin` 폴더에 새 페이지에 바인딩할 수 있도록는 `Site.master` 마스터 페이지입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-131">Next, add a content page named `Default.aspx` to the `Admin` folder, making sure to bind the new page to the `Site.master` master page.</span></span>

> [!NOTE]
> <span data-ttu-id="76cfd-132">에 [ *마스터 페이지에서 제목, 메타 태그 및 기타 HTML 헤더 지정* ](specifying-the-title-meta-tags-and-other-html-headers-in-the-master-page-vb.md) 자습서 라는 기본 페이지를 사용자 지정 클래스를 만들었습니다 `BasePage` 콘텐츠 페이지의 제목을 자동으로 설정 하는 (하는 경우 해당 명시적으로 할당 되지 않았습니다)입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-132">In the [*Specifying the Title, Meta Tags, and Other HTML Headers in the Master Page*](specifying-the-title-meta-tags-and-other-html-headers-in-the-master-page-vb.md) tutorial we created a custom base page class named `BasePage` that automatically set the content page's title (if it was not explicitly assigned).</span></span> <span data-ttu-id="76cfd-133">새로 생성된 된 페이지의 코드 숨김 클래스에서 파생 해야 `BasePage` 이 기능을 활용할 수 있도록 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-133">Don't forget to have the newly created page's code-behind class derive from `BasePage` so that it can utilize this functionality.</span></span>


<span data-ttu-id="76cfd-134">이 콘텐츠 페이지를 만든 후 솔루션 탐색기는 그림 1 유사 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-134">After you have created this content page, your Solution Explorer should look similar to Figure 1.</span></span>


![새 폴더 및 ASP.NET 페이지 프로젝트에 추가 되었습니다.](urls-in-master-pages-vb/_static/image1.png)

<span data-ttu-id="76cfd-136">**그림 01**: 새 폴더 및 ASP.NET 페이지 프로젝트에 추가 되었습니다</span><span class="sxs-lookup"><span data-stu-id="76cfd-136">**Figure 01**: A New Folder and ASP.NET Page Have Been Added to the Project</span></span>


<span data-ttu-id="76cfd-137">다음으로 업데이트 합니다 `Web.sitemap` 포함할 새 파일을 `<siteMapNode>` 이 단원에 대 한 항목입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-137">Next, update the `Web.sitemap` file to include a new `<siteMapNode>` entry for this lesson.</span></span> <span data-ttu-id="76cfd-138">다음 XML에서는 전체를 보여 줍니다 `Web.sitemap` 이제는 제 3의 추가 포함 하는 태그 `<siteMapNode>` 요소입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-138">The following XML shows the complete `Web.sitemap` markup, which now includes the addition of a third `<siteMapNode>` element.</span></span>


[!code-xml[Main](urls-in-master-pages-vb/samples/sample3.xml)]

<span data-ttu-id="76cfd-139">새로 만든 `Default.aspx` 페이지에는 네 가지 콘텐츠 컨트롤에 해당 하는 네 가지 ContentPlaceHolders에서 있어야 `Site.master`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-139">The newly created `Default.aspx` page should have four Content controls corresponding to the four ContentPlaceHolders in `Site.master`.</span></span> <span data-ttu-id="76cfd-140">참조 하는 콘텐츠 컨트롤에 일부 텍스트를 추가 합니다 `MainContent` ContentPlaceHolder 한 후 브라우저를 통해 페이지를 방문 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-140">Add some text to the Content control referencing the `MainContent` ContentPlaceHolder and then visit the page through a browser.</span></span> <span data-ttu-id="76cfd-141">그림 2에서 알 수 있듯이, 브라우저를 찾을 수 없습니다는 `PoweredByASPNET.gif` 이미지 파일입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-141">As Figure 2 shows, the browser cannot find the `PoweredByASPNET.gif` image file.</span></span> <span data-ttu-id="76cfd-142">여기에 무슨 일이 일어나고 있나요?</span><span class="sxs-lookup"><span data-stu-id="76cfd-142">What's going on here?</span></span>

<span data-ttu-id="76cfd-143">합니다 `~/Admin/Default.aspx` 콘텐츠 페이지에 대 한 동일한 HTML 전송 되는 `footerContent` 지역 처럼를 `About.aspx` 페이지:</span><span class="sxs-lookup"><span data-stu-id="76cfd-143">The `~/Admin/Default.aspx` content page is sent the same HTML for the `footerContent` region as was the `About.aspx` page:</span></span>


[!code-html[Main](urls-in-master-pages-vb/samples/sample4.html)]

<span data-ttu-id="76cfd-144">때문에 `<img>` 요소의 `src` 특성은 상대 URL을 브라우저 표시 하려고를 `Images` 웹 페이지의 폴더 위치를 기준으로 폴더입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-144">Because the `<img>` element's `src` attribute is a relative URL, the browser attempts to look for an `Images` folder relative to the web page's folder location.</span></span> <span data-ttu-id="76cfd-145">즉, 브라우저 이미지 파일을 찾고 `Admin/Images/PoweredByASPNET.gif`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-145">In other words, the browser is looking for the image file `Admin/Images/PoweredByASPNET.gif`.</span></span>


<span data-ttu-id="76cfd-146">[![PoweredByASPNET.gif 이미지 파일을 찾을 수 없습니다.](urls-in-master-pages-vb/_static/image3.png)](urls-in-master-pages-vb/_static/image2.png)</span><span class="sxs-lookup"><span data-stu-id="76cfd-146">[![The PoweredByASPNET.gif Image File Cannot Be Found](urls-in-master-pages-vb/_static/image3.png)](urls-in-master-pages-vb/_static/image2.png)</span></span>

<span data-ttu-id="76cfd-147">**그림 02**: 합니다 `PoweredByASPNET.gif` 이미지 파일을 찾을 수 없습니다 ([클릭 하 여 큰 이미지 보기](urls-in-master-pages-vb/_static/image4.png))</span><span class="sxs-lookup"><span data-stu-id="76cfd-147">**Figure 02**: The `PoweredByASPNET.gif` Image File Cannot Be Found  ([Click to view full-size image](urls-in-master-pages-vb/_static/image4.png))</span></span>


### <a name="replacing-relative-urls-with-absolute-urls"></a><span data-ttu-id="76cfd-148">절대 Url을 사용 하 여 상대 Url을으로 바꿉니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-148">Replacing Relative URLs with Absolute URLs</span></span>

<span data-ttu-id="76cfd-149">상대 URL의 반대 되는 *절대 URL*, 슬래시로 시작 되는 (`/`) 또는와 같은 프로토콜 `http://`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-149">The opposite of a relative URL is an *absolute URL*, which is one that starts with a forward slash (`/`) or a protocol such as `http://`.</span></span> <span data-ttu-id="76cfd-150">알려진된 고정된 지점에서 리소스의 위치를 지정 하는 절대 URL을 하기 때문에 같은 절대 URL을 웹 사이트의 폴더 구조에서 웹 페이지의 위치에 관계 없이 모든 웹 페이지에 유효 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-150">Because an absolute URL specifies the location of a resource from a known fixed point, the same absolute URL is valid in any web page, regardless of the web page's location in the website's folder structure.</span></span>

<span data-ttu-id="76cfd-151">그림 2 에서처럼 깨진된 이미지를 해결 하려면 업데이트 해야 합니다 `<img>` 요소의 `src` 특성 절대 URL을 상대 대신 사용할 수 있도록 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-151">To remedy the broken image shown in Figure 2, we need to update the `<img>` element's `src` attribute so that it uses an absolute URL instead of a relative one.</span></span> <span data-ttu-id="76cfd-152">올바른 절대 URL을 확인 하려면 웹 사이트의 웹 페이지 중 하나를 방문 하 고 주소 표시줄을 검사 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-152">To determine the correct absolute URL, visit one of the web pages in your website and examine the Address bar.</span></span> <span data-ttu-id="76cfd-153">웹 응용 프로그램의 정규화 된 경로 그림 2의 주소 표시줄에서 알 수 있듯이, `http://localhost:3908/ASPNET_MasterPages_Tutorial_04_VB/`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-153">As the Address bar in Figure 2 shows, the fully qualified path to the web application is `http://localhost:3908/ASPNET_MasterPages_Tutorial_04_VB/`.</span></span> <span data-ttu-id="76cfd-154">따라서 업데이트 수를 `<img>` 요소의 `src` 특성을 다음 두 개의 절대 Url 중 하나:</span><span class="sxs-lookup"><span data-stu-id="76cfd-154">Therefore, we could update the `<img>` element's `src` attribute to either of the following two absolute URLs:</span></span>

- `/ASPNET_MasterPages_Tutorial_04_VB/Images/PoweredByASPNET.gif`
- `http://localhost:3908/ASPNET_MasterPages_Tutorial_04_VB/Images/PoweredByASPNET.gif`

<span data-ttu-id="76cfd-155">잠시 업데이트 하는 `<img>` 요소의 `src` 위에 표시 된 형식 중 하나를 사용 하 여 절대 url 특성 및 다음 방문 하 여는 `~/Admin/Default.aspx` 브라우저를 통해 페이지입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-155">Take a moment to update the `<img>` element's `src` attribute to an absolute URL using one of the forms shown above and then visit the `~/Admin/Default.aspx` page through a browser.</span></span> <span data-ttu-id="76cfd-156">브라우저를 제대로 찾아 표시할이 이번을 `PoweredByASPNET.gif` 이미지 파일 (그림 3 참조).</span><span class="sxs-lookup"><span data-stu-id="76cfd-156">This time the browser will correctly find and display the `PoweredByASPNET.gif` image file (see Figure 3).</span></span>


<span data-ttu-id="76cfd-157">[![PoweredByASPNET.gif 이미지는 이제 표시](urls-in-master-pages-vb/_static/image6.png)](urls-in-master-pages-vb/_static/image5.png)</span><span class="sxs-lookup"><span data-stu-id="76cfd-157">[![The PoweredByASPNET.gif Image is Now Displayed](urls-in-master-pages-vb/_static/image6.png)](urls-in-master-pages-vb/_static/image5.png)</span></span>

<span data-ttu-id="76cfd-158">**그림 03**: 합니다 `PoweredByASPNET.gif` 이미지가 표시 됩니다 ([클릭 하 여 큰 이미지 보기](urls-in-master-pages-vb/_static/image7.png))</span><span class="sxs-lookup"><span data-stu-id="76cfd-158">**Figure 03**: The `PoweredByASPNET.gif` Image is Now Displayed  ([Click to view full-size image](urls-in-master-pages-vb/_static/image7.png))</span></span>


<span data-ttu-id="76cfd-159">절대 URL 하드 코딩 작동 하는 동안이 밀접 하 게 결합 HTML 웹 사이트의 서버 및 폴더 위치를 변경할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-159">While hard coding in the absolute URL works, it tightly couples your HTML to the website's server and folder location, which may change.</span></span> <span data-ttu-id="76cfd-160">폼의 절대 URL을 사용 하 여 `http://localhost:3908/...` 불안정 이므로 localhost 앞에 포트 번호를 자동으로 Visual Studio의 기본 제공 ASP.NET 개발 웹 서버 시작 될 때마다 선택 됩니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-160">Using an absolute URL of the form `http://localhost:3908/...` is brittle because the port number preceding localhost is selected automatically each time Visual Studio's built-in ASP.NET Development Web Server is started.</span></span> <span data-ttu-id="76cfd-161">마찬가지로,는 `http://localhost` 파트 때만 유효 로컬로 테스트 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-161">Similarly, the `http://localhost` part is only valid when testing locally.</span></span> <span data-ttu-id="76cfd-162">URL 기본 바뀌어 다른 항목에 같은 코드를 프로덕션 서버로 배포 되 면 `http://www.yourserver.com`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-162">Once the code is deployed to a production server, the URL base will change to something else, like `http://www.yourserver.com`.</span></span> <span data-ttu-id="76cfd-163">폼의 절대 URL `/ASPNET_MasterPages_Tutorial_04_VB/...` 도 동일한 때에서 증가 하므로 문제가 발생이 응용 프로그램 경로 개발 및 프로덕션 서버 간의 다른 경우가 많습니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-163">The absolute URL in the form `/ASPNET_MasterPages_Tutorial_04_VB/...` also suffers from the same brittleness because oftentimes this application path differs between development and production servers.</span></span>

<span data-ttu-id="76cfd-164">좋은 소식은 ASP.NET 런타임에 유효한 상대 URL을 생성 하는 방법을 제공 하는 것입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-164">The good news is that ASP.NET offers a method for generating a valid relative URL at runtime.</span></span>

## <a name="usingandresolveclienturl"></a><span data-ttu-id="76cfd-165">Using`~`and`ResolveClientUrl`</span><span class="sxs-lookup"><span data-stu-id="76cfd-165">Using`~`and`ResolveClientUrl`</span></span>

<span data-ttu-id="76cfd-166">대신 절대 URL를 하드 코딩 보다 ASP.NET을 사용 하면 페이지 개발자가 물결표를 사용 하도록 (`~`) 웹 응용 프로그램의 루트를 나타냅니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-166">Rather than hard code an absolute URL, ASP.NET allows page developers to use the tilde (`~`) to indicate the root of the web application.</span></span> <span data-ttu-id="76cfd-167">예를 들어, 표기법 사용이 자습서의 앞부분에 나오는 `~/Admin/Default.aspx` 참조 텍스트에는 `Default.aspx` 페이지에 `Admin` 폴더.</span><span class="sxs-lookup"><span data-stu-id="76cfd-167">For example, earlier in this tutorial I used the notation `~/Admin/Default.aspx` in the text to refer to the `Default.aspx` page in the `Admin` folder.</span></span> <span data-ttu-id="76cfd-168">합니다 `~` 나타냅니다는 `Admin` 폴더가 웹 응용 프로그램의 루트의 하위 폴더입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-168">The `~` indicates that the `Admin` folder is a subfolder of the web application's root.</span></span>

<span data-ttu-id="76cfd-169">합니다 `Control` 클래스의 [ `ResolveClientUrl` 메서드](https://msdn.microsoft.com/library/system.web.ui.control.resolveclienturl.aspx) URL을 사용 하 고 컨트롤이 상주 하는 웹 페이지에 대 한 적절 한 상대 URL을 수정 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-169">The `Control` class's [`ResolveClientUrl` method](https://msdn.microsoft.com/library/system.web.ui.control.resolveclienturl.aspx) takes a URL and modifies it to a relative URL appropriate for the web page on which the control resides.</span></span> <span data-ttu-id="76cfd-170">예를 들어, 호출 `ResolveClientUrl("~/Images/PoweredByASPNET.gif")` 에서 `About.aspx` 반환 `Images/PoweredByASPNET.gif`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-170">For example, calling `ResolveClientUrl("~/Images/PoweredByASPNET.gif")` from `About.aspx` returns `Images/PoweredByASPNET.gif`.</span></span> <span data-ttu-id="76cfd-171">하지만 호출한 `~/Admin/Default.aspx`를 반환 `../Images/PoweredByASPNET.gif`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-171">Calling it from `~/Admin/Default.aspx`, however, returns `../Images/PoweredByASPNET.gif`.</span></span>

> [!NOTE]
> <span data-ttu-id="76cfd-172">모든 ASP.NET 서버 컨트롤에서 파생 되므로 합니다 `Control` 클래스를 모든 서버 컨트롤에 액세스할 수는 `ResolveClientUrl` 메서드.</span><span class="sxs-lookup"><span data-stu-id="76cfd-172">Because all ASP.NET server controls derive from the `Control` class, all server controls have access to the `ResolveClientUrl` method.</span></span> <span data-ttu-id="76cfd-173">도 `Page` 클래스에서 파생 되는 `Control` 클래스, 즉 ASP.NET 페이지의 코드 숨김 클래스에서 직접이 메서드를 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-173">Even the `Page` class derives from the `Control` class, meaning that you can use this method directly from your ASP.NET pages' code-behind classes.</span></span>


### <a name="usingin-the-declarative-markup"></a><span data-ttu-id="76cfd-174">사용 하 여`~`선언적 태그에</span><span class="sxs-lookup"><span data-stu-id="76cfd-174">Using`~`in the Declarative Markup</span></span>

<span data-ttu-id="76cfd-175">URL 관련 속성을 포함 하는 여러 ASP.NET 웹 컨트롤로: 하이퍼링크 컨트롤에는 `NavigateUrl` 속성, 컨트롤에 이미지를 `ImageUrl` 속성 등에입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-175">Several ASP.NET Web controls include URL-related properties: the HyperLink control has a `NavigateUrl` property; the Image control has an `ImageUrl` property; and so on.</span></span> <span data-ttu-id="76cfd-176">이러한 컨트롤에 해당 URL 관련 속성 값을 전달할를 렌더링할 때 `ResolveClientUrl`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-176">When rendered, these controls pass their URL-related property values to `ResolveClientUrl`.</span></span> <span data-ttu-id="76cfd-177">따라서 이러한 속성을 포함 하는 경우는 `~` 웹 응용 프로그램의 루트를 가리키는 URL 유효한 상대 URL로 수정 됩니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-177">Consequently, if these properties contain a `~` to indicate the root of the web application, the URL will be modified to a valid relative URL.</span></span>

<span data-ttu-id="76cfd-178">ASP.NET 서버 컨트롤에만 변환 하는 염두에 둡니다는 `~` 해당 URL 관련 속성에서입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-178">Keep in mind that only ASP.NET server controls transform the `~` in their URL-related properties.</span></span> <span data-ttu-id="76cfd-179">경우는 `~` 와 같은 정적 HTML 태그에 표시 됩니다 `<img src="~/Images/PoweredByASPNET.gif" />`, ASP.NET 엔진은 전송는 `~` 나머지 HTML 콘텐츠와 함께 브라우저로 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-179">If a `~` appears in static HTML markup, such as `<img src="~/Images/PoweredByASPNET.gif" />`, the ASP.NET engine sends the `~` to the browser along with the rest of the HTML content.</span></span> <span data-ttu-id="76cfd-180">브라우저에 있다고 가정 합니다 `~` URL의 일부입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-180">The browser assumes that the `~` is part of the URL.</span></span> <span data-ttu-id="76cfd-181">예를 들어 태그를 수신 하는 브라우저 `<img src="~/Images/PoweredByASPNET.gif" />` 라는 하위 폴더는 가정 `~` 하위 폴더를 사용 하 여 `Images` 이미지 파일이 포함 된 `PoweredByASPNET.gif`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-181">For example, if the browser receives the markup `<img src="~/Images/PoweredByASPNET.gif" />` it assumes there is a subfolder named `~` with a subfolder `Images` that contains the image file `PoweredByASPNET.gif`.</span></span>

<span data-ttu-id="76cfd-182">이미지 태그를 해결 하려면 `Site.master`, 기존 대체 `<img>` ASP.NET 이미지 웹 컨트롤을 사용 하 여 요소입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-182">To fix the image markup in `Site.master`, replace the existing `<img>` element with an ASP.NET Image Web control.</span></span> <span data-ttu-id="76cfd-183">이미지 웹 컨트롤의 설정 `ID` 하 `PoweredByImage`, 해당 `ImageUrl` 속성을 `~/Images/PoweredByASPNET.gif`, 및 해당 `AlternateText` "ASP.NET에서 전원!" 속성</span><span class="sxs-lookup"><span data-stu-id="76cfd-183">Set the Image Web control's `ID` to `PoweredByImage`, its `ImageUrl` property to `~/Images/PoweredByASPNET.gif`, and its `AlternateText` property to "Powered by ASP.NET!"</span></span>


[!code-aspx[Main](urls-in-master-pages-vb/samples/sample5.aspx)]

<span data-ttu-id="76cfd-184">이 변경의 마스터 페이지에 확인 한 후 다시 방문는 `~/Admin/Default.aspx` 페이지를 다시 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-184">After making this change to the master page, revisit the `~/Admin/Default.aspx` page again.</span></span> <span data-ttu-id="76cfd-185">이 이번에는 `PoweredByASPNET.gif` 이미지 파일 페이지에 나타납니다 (그림 3 참조).</span><span class="sxs-lookup"><span data-stu-id="76cfd-185">This time the `PoweredByASPNET.gif` image file appears in the page (see Figure 3).</span></span> <span data-ttu-id="76cfd-186">이미지 웹 컨트롤을 렌더링할 때이 사용 합니다 `ResolveClientUrl` 해결 방법 해당 `ImageUrl` 속성 값입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-186">When the Image Web control is rendered it uses the `ResolveClientUrl` method to resolve its `ImageUrl` property value.</span></span> <span data-ttu-id="76cfd-187">`~/Admin/Default.aspx` 는 `ImageUrl` 을 적절 한 상대 URL의 HTML 소스 다음 코드 조각으로 변환 됩니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-187">In `~/Admin/Default.aspx` the `ImageUrl` is converted into an appropriate relative URL, as the following snippet of HTML source shows:</span></span>


[!code-html[Main](urls-in-master-pages-vb/samples/sample6.html)]

> [!NOTE]
> <span data-ttu-id="76cfd-188">URL 기반 웹 컨트롤 속성에 사용 될 뿐만 아니라 합니다 `~` 호출 하는 경우에 사용할 수는 `Response.Redirect` 및 `Server.MapPath` 특히 메서드.</span><span class="sxs-lookup"><span data-stu-id="76cfd-188">In addition to being used in URL-based Web control properties, the `~` can also be used when calling the `Response.Redirect` and `Server.MapPath` methods, among others.</span></span> <span data-ttu-id="76cfd-189">또한 합니다 `ResolveClientUrl` 필요한 경우 ASP.NET 또는 마스터 페이지의 선언적 태그에서 직접 메서드를 호출할 수 있습니다를 참조 하세요 [Fritz Onion](https://www.pluralsight.com/blogs/fritz/)의 블로그 항목 [사용 하 여 `ResolveClientUrl` 태그에서](https://www.pluralsight.com/blogs/fritz/archive/2006/02/06/18596.aspx)합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-189">Also, the `ResolveClientUrl` method may be invoked directly from an ASP.NET or master page's declarative markup, if needed; see [Fritz Onion](https://www.pluralsight.com/blogs/fritz/)'s blog entry [Using `ResolveClientUrl` in Markup](https://www.pluralsight.com/blogs/fritz/archive/2006/02/06/18596.aspx).</span></span>


## <a name="fixing-the-master-pages-remaining-relative-urls"></a><span data-ttu-id="76cfd-190">마스터 페이지의 상대 Url을 나머지 수정</span><span class="sxs-lookup"><span data-stu-id="76cfd-190">Fixing the Master Page's Remaining Relative URLs</span></span>

<span data-ttu-id="76cfd-191">외에 `<img>` 요소에는 `footerContent` 방금 고정을 마스터 페이지는 주의 기울여야 하는 하나 이상의 상대 URL이 포함 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-191">In addition to the `<img>` element in the `footerContent` that we just fixed, the master page contains one more relative URL that requires our attention.</span></span> <span data-ttu-id="76cfd-192">합니다 `topContent` "마스터 페이지 자습서"를 가리키는 링크를 포함 하는 지역 `Default.aspx`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-192">The `topContent` region includes the link "Master Pages Tutorials," which points to `Default.aspx`.</span></span>


[!code-html[Main](urls-in-master-pages-vb/samples/sample7.html)]

<span data-ttu-id="76cfd-193">사용자가이 URL을 상대 하기 때문에 전송할는 `Default.aspx` 콘텐츠 페이지를 방문 하는 것의 폴더에는 페이지입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-193">Because this URL is relative, it will send the user to the `Default.aspx` page in the folder of the content page they are visiting.</span></span> <span data-ttu-id="76cfd-194">항상이 링크를 `Default.aspx` 바꾸려면 먼저 루트 폴더에는 `<a>` 하이퍼링크 웹을 사용 하 여 요소를 사용할 수 있도록 제어는 `~` 표기법.</span><span class="sxs-lookup"><span data-stu-id="76cfd-194">To have this link always point to `Default.aspx` in the root folder we need to replace the `<a>` element with a HyperLink Web control so that we can use the `~` notation.</span></span>

<span data-ttu-id="76cfd-195">제거는 `<a>` 요소 태그 및 해당 위치에 하이퍼링크 컨트롤을 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-195">Remove the `<a>` element markup and add a HyperLink control in its place.</span></span> <span data-ttu-id="76cfd-196">하이퍼링크의 설정 `ID` 하 `lnkHome`, 해당 `NavigateUrl` 속성을 `~/Default.aspx`, 및 해당 `Text` 속성을 "마스터 페이지 자습서입니다."</span><span class="sxs-lookup"><span data-stu-id="76cfd-196">Set the HyperLink's `ID` to `lnkHome`, its `NavigateUrl` property to `~/Default.aspx`, and its `Text` property to "Master Pages Tutorials."</span></span>


[!code-aspx[Main](urls-in-master-pages-vb/samples/sample8.aspx)]

<span data-ttu-id="76cfd-197">정말 간단하죠.</span><span class="sxs-lookup"><span data-stu-id="76cfd-197">That's it!</span></span> <span data-ttu-id="76cfd-198">이 시점에 마스터 페이지 및 콘텐츠 페이지를 폴더에 관계 없이 콘텐츠 페이지에 의해 렌더링 되는 경우에 제대로 Url 마스터 페이지에 기반한 모든이 있습니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-198">At this point all the URLs in our master page are properly based when rendered by a content page regardless of what folders the master page and content page are located in.</span></span>

### <a name="automatic-url-resolution-in-theheadsection"></a><span data-ttu-id="76cfd-199">자동 URL 해상도`<head>`섹션</span><span class="sxs-lookup"><span data-stu-id="76cfd-199">Automatic URL Resolution in the`<head>`Section</span></span>

<span data-ttu-id="76cfd-200">에 [ *사이트 전체 레이아웃을 사용 하 여 마스터 페이지 만들기* ](creating-a-site-wide-layout-using-master-pages-vb.md) 추가한 자습서를 `<link>` 에 `Styles.css` 파일는 `<head>` 지역:</span><span class="sxs-lookup"><span data-stu-id="76cfd-200">In the [*Creating a Site-Wide Layout Using Master Pages*](creating-a-site-wide-layout-using-master-pages-vb.md) tutorial we added a `<link>` to the `Styles.css` file in the `<head>` region:</span></span>


[!code-aspx[Main](urls-in-master-pages-vb/samples/sample9.aspx)]

<span data-ttu-id="76cfd-201">동안 합니다 `<link>` 요소의 `href` 특성은 상대, 런타임 시 적절 한 경로 자동으로 변환 됩니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-201">While the `<link>` element's `href` attribute is relative, it's automatically converted to an appropriate path at runtime.</span></span> <span data-ttu-id="76cfd-202">설명한 대로 합니다 [ *마스터 페이지에서 제목, 메타 태그 및 기타 HTML 헤더 지정* ](specifying-the-title-meta-tags-and-other-html-headers-in-the-master-page-vb.md) 자습서는 `<head>` 지역은 실제로 서버 쪽 컨트롤을 수정할 수 있습니다는 렌더링 될 때 해당 내부 컨트롤의 내용입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-202">As we discussed in the [*Specifying the Title, Meta Tags, and Other HTML Headers in the Master Page*](specifying-the-title-meta-tags-and-other-html-headers-in-the-master-page-vb.md) tutorial, the `<head>` region is actually a server-side control, which enables it to modify the contents of its inner controls when it is rendered.</span></span>

<span data-ttu-id="76cfd-203">이 확인 하려면 다시는 `~/Admin/Default.aspx` 페이지 및 브라우저에 보내지는 HTML 소스를 보고 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-203">To verify this, revisit the `~/Admin/Default.aspx` page and view the HTML source sent to the browser.</span></span> <span data-ttu-id="76cfd-204">아래 코드 조각에서 볼 수 있듯이 합니다 `<link>` 요소의 `href` 특성을 적절 한 상대 URL을 자동으로 수정 된 `../Styles.css`합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-204">As the snippet below illustrates, the `<link>` element's `href` attribute has been automatically modified to an appropriate relative URL, `../Styles.css`.</span></span>


[!code-html[Main](urls-in-master-pages-vb/samples/sample10.html)]

## <a name="summary"></a><span data-ttu-id="76cfd-205">요약</span><span class="sxs-lookup"><span data-stu-id="76cfd-205">Summary</span></span>

<span data-ttu-id="76cfd-206">마스터 페이지 링크, 이미지 및 URL을 통해 지정 해야 하는 다른 외부 리소스에 자주 포함 됩니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-206">Master pages very often include links, images, and other external resources that must be specified via a URL.</span></span> <span data-ttu-id="76cfd-207">마스터 페이지 및 콘텐츠 페이지를 같은 폴더에 존재 하지 않을 이므로 abstain 상대 Url을 사용 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-207">Because the master page and content pages might not exist in the same folder, it is important to abstain from using relative URLs.</span></span> <span data-ttu-id="76cfd-208">하드 코드 된 절대 Url을 사용할 수 있지만, 웹 응용 프로그램 절대 URL을 결합 하므로 긴밀 하 게 수행 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-208">While it is possible to use hard coded absolute URLs, doing so tightly couples the absolute URL to the web application.</span></span> <span data-ttu-id="76cfd-209">절대 URL 변경 처럼 자주 이동 또는 웹 응용 프로그램-배포-돌아가서 절대 Url을 업데이트 해야 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-209">If the absolute URL changes - as it often does when moving or deploying a web application - you'll have to remember to go back and update the absolute URLs.</span></span>

<span data-ttu-id="76cfd-210">물결표를 사용 하는 이상적인 방법입니다 (`~`) 응용 프로그램 루트를 나타냅니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-210">The ideal approach is to use the tilde (`~`) to indicate the application root.</span></span> <span data-ttu-id="76cfd-211">URL 관련 속성을 포함 하는 ASP.NET 웹 컨트롤로 매핑하는 `~` 런타임 시 응용 프로그램 루트에.</span><span class="sxs-lookup"><span data-stu-id="76cfd-211">ASP.NET Web controls that contain URL-related properties map the `~` to the application root at runtime.</span></span> <span data-ttu-id="76cfd-212">웹 컨트롤 사용 하 여 내부적으로 `Control` 클래스의 `ResolveClientUrl` 유효한 상대 URL을 생성 하는 방법입니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-212">Internally, the Web controls use the `Control` class's `ResolveClientUrl` method to generate a valid relative URL.</span></span> <span data-ttu-id="76cfd-213">이 메서드는 공용 및 모든 서버 컨트롤에서 사용할 수 있습니다 (포함 하는 `Page` 클래스) 이므로 사용할 수 있습니다 프로그래밍 방식으로 코드 숨김 클래스에서 필요한 경우.</span><span class="sxs-lookup"><span data-stu-id="76cfd-213">This method is public and available from every server control (including the `Page` class), so you can use it programmatically from your code-behind classes, if needed.</span></span>

<span data-ttu-id="76cfd-214">즐거운 프로그래밍!</span><span class="sxs-lookup"><span data-stu-id="76cfd-214">Happy Programming!</span></span>

### <a name="further-reading"></a><span data-ttu-id="76cfd-215">추가 정보</span><span class="sxs-lookup"><span data-stu-id="76cfd-215">Further Reading</span></span>

<span data-ttu-id="76cfd-216">이 자습서에서 다루는 항목에 대 한 자세한 내용은 다음 리소스를 참조 하세요.</span><span class="sxs-lookup"><span data-stu-id="76cfd-216">For more information on the topics discussed in this tutorial, refer to the following resources:</span></span>

- [<span data-ttu-id="76cfd-217">ASP.NET의 마스터 페이지</span><span class="sxs-lookup"><span data-stu-id="76cfd-217">Master Pages in ASP.NET</span></span>](http://www.odetocode.com/Articles/419.aspx)
- [<span data-ttu-id="76cfd-218">마스터 페이지에서 기준 주소 다시 지정 URL</span><span class="sxs-lookup"><span data-stu-id="76cfd-218">URL Rebasing in a Master Page</span></span>](https://quickstarts.asp.net/QuickStartv20/aspnet/doc/masterpages/default.aspx#urls)
- [<span data-ttu-id="76cfd-219">사용 하 여 `ResolveClientUrl` 태그</span><span class="sxs-lookup"><span data-stu-id="76cfd-219">Using `ResolveClientUrl` in Markup</span></span>](https://www.pluralsight.com/blogs/fritz/archive/2006/02/06/18596.aspx)

### <a name="about-the-author"></a><span data-ttu-id="76cfd-220">저자 소개</span><span class="sxs-lookup"><span data-stu-id="76cfd-220">About the Author</span></span>

<span data-ttu-id="76cfd-221">[Scott Mitchell](http://www.4guysfromrolla.com/ScottMitchell.shtml), 작성자의 여러 ASP/ASP.NET 서적과 4GuysFromRolla.com의 설립자 이며, 왔습니다 Microsoft 웹 기술을 1998 합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-221">[Scott Mitchell](http://www.4guysfromrolla.com/ScottMitchell.shtml), author of multiple ASP/ASP.NET books and founder of 4GuysFromRolla.com, has been working with Microsoft Web technologies since 1998.</span></span> <span data-ttu-id="76cfd-222">Scott는 독립 컨설턴트, 강사, 그리고 기록기로 작동합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-222">Scott works as an independent consultant, trainer, and writer.</span></span> <span data-ttu-id="76cfd-223">최근 저서는 [ *Sams 설명 직접 ASP.NET 3.5 24 시간 동안의*](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco)합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-223">His latest book is [*Sams Teach Yourself ASP.NET 3.5 in 24 Hours*](https://www.amazon.com/exec/obidos/ASIN/0672327384/4guysfromrollaco).</span></span> <span data-ttu-id="76cfd-224">Scott에 도달할 수 있습니다 [ mitchell@4GuysFromRolla.com ](mailto:mitchell@4GuysFromRolla.com) 통하거나 저자의 블로그 [ http://ScottOnWriting.NET ](http://scottonwriting.net/)합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-224">Scott can be reached at [mitchell@4GuysFromRolla.com](mailto:mitchell@4GuysFromRolla.com) or via his blog at [http://ScottOnWriting.NET](http://scottonwriting.net/).</span></span>

### <a name="special-thanks-to"></a><span data-ttu-id="76cfd-225">특별히 감사</span><span class="sxs-lookup"><span data-stu-id="76cfd-225">Special Thanks To</span></span>

<span data-ttu-id="76cfd-226">내 향후 MSDN 문서를 검토에 관심이 있으십니까?</span><span class="sxs-lookup"><span data-stu-id="76cfd-226">Interested in reviewing my upcoming MSDN articles?</span></span> <span data-ttu-id="76cfd-227">그렇다면 삭제 나에서 선 [ mitchell@4GuysFromRolla.com ](mailto:mitchell@4GuysFromRolla.com)합니다.</span><span class="sxs-lookup"><span data-stu-id="76cfd-227">If so, drop me a line at [mitchell@4GuysFromRolla.com](mailto:mitchell@4GuysFromRolla.com).</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="76cfd-228">[이전](specifying-the-title-meta-tags-and-other-html-headers-in-the-master-page-vb.md)
> [다음](control-id-naming-in-content-pages-vb.md)</span><span class="sxs-lookup"><span data-stu-id="76cfd-228">[Previous](specifying-the-title-meta-tags-and-other-html-headers-in-the-master-page-vb.md)
[Next](control-id-naming-in-content-pages-vb.md)</span></span>
