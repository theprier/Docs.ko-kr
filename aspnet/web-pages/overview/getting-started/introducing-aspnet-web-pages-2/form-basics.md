---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: HTML 양식 기본 ASP.NET Web Pages-소개 | Microsoft Docs
author: tfitzmac
description: 이 자습서에서는 입력된 폼을 만드는 방법 및 ASP.NET Web Pages (Razor)를 사용 하는 경우 사용자의 입력을 처리 하는 방법의 기본 사항을 보여 줍니다. 그리고 이제...
ms.author: aspnetcontent
manager: wpickett
ms.date: 05/28/2015
ms.topic: article
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
ms.technology: dotnet-webpages
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f5f7c9813041c443675f4e443822a81c1c508c20
ms.sourcegitcommit: 953ff9ea4369f154d6fd0239599279ddd3280009
ms.translationtype: MT
ms.contentlocale: ko-KR
ms.lasthandoff: 07/03/2018
ms.locfileid: "37391993"
---
<a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="c19b8-104">HTML 양식 기본 ASP.NET 웹 페이지 소개</span><span class="sxs-lookup"><span data-stu-id="c19b8-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>
====================
<span data-ttu-id="c19b8-105">[Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="c19b8-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="c19b8-106">이 자습서에서는 입력된 폼을 만드는 방법 및 ASP.NET Web Pages (Razor)를 사용 하는 경우 사용자의 입력을 처리 하는 방법의 기본 사항을 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="c19b8-107">및 데이터베이스에서 가져온 했으므로 폼 기술을 사용자 데이터베이스에서 특정 영화를 찾을 수 있도록 사용할지 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="c19b8-108">통해 시리즈를 완료 했다고 가정 하 [소개에 표시할 데이터를 사용 하 여 ASP.NET 웹 페이지](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data)합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="c19b8-109">학습할 내용:</span><span class="sxs-lookup"><span data-stu-id="c19b8-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="c19b8-110">표준 HTML 요소를 사용 하 여 폼을 만드는 방법.</span><span class="sxs-lookup"><span data-stu-id="c19b8-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="c19b8-111">사용자를 읽는 방법의 형태로 입력 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="c19b8-112">선택적으로 검색을 사용 하 여 데이터를 가져오면 용어는 사용자는 SQL 쿼리를 만드는 방법을 제공 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="c19b8-113">페이지에서 "기억"를 입력 한 필드가 포함 하는 방법.</span><span class="sxs-lookup"><span data-stu-id="c19b8-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="c19b8-114">기능/기술을 설명 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="c19b8-115">`Request` 개체</span><span class="sxs-lookup"><span data-stu-id="c19b8-115">The `Request` object.</span></span>
> - <span data-ttu-id="c19b8-116">SQL `Where` 절.</span><span class="sxs-lookup"><span data-stu-id="c19b8-116">The SQL `Where` clause.</span></span>


## <a name="what-youll-build"></a><span data-ttu-id="c19b8-117">만들 내용</span><span class="sxs-lookup"><span data-stu-id="c19b8-117">What You'll Build</span></span>

<span data-ttu-id="c19b8-118">이전 자습서에서 만들고 데이터베이스, 데이터를 추가한 다음 사용 하 여 `WebGrid` 데이터를 표시 하는 도우미입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="c19b8-119">이 자습서에서는 해당 제목을 입력할 모든 단어를 포함 또는 동영상의 특정 장르를 찾을 수 있는 검색 상자를 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="c19b8-120">(예를 들어, 있습니다 수 장르가 "Action" 이거나 해당 제목 "Harry" 또는 "Adventure."를 포함 하는 모든 동영상 찾기)</span><span class="sxs-lookup"><span data-stu-id="c19b8-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="c19b8-121">이 자습서를 완료 하는 경우이 이와 유사를 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![영화 장르 및 제목 검색 페이지](form-basics/_static/image1.png)

<span data-ttu-id="c19b8-123">페이지의 목록 일부는 마지막 자습서 동일 &mdash; 표입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="c19b8-124">차이 표에 나타납니다 영화만 검색 한 됩니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="c19b8-125">HTML 폼에 대 한</span><span class="sxs-lookup"><span data-stu-id="c19b8-125">About HTML Forms</span></span>

<span data-ttu-id="c19b8-126">(HTML 폼 만들기와 차이점을 사용 하 여 환경이 있다면 `GET` 고 `POST`,이 섹션을 건너뛸 수 있습니다.)</span><span class="sxs-lookup"><span data-stu-id="c19b8-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="c19b8-127">폼에는 사용자 입력된 요소의 &mdash; 텍스트 상자, 단추, 라디오 단추, 확인란, 드롭다운 목록 및 등입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="c19b8-128">사용자 이러한 컨트롤 입력 또는 선택 하 고 단추를 클릭 하 여 폼을 제출 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="c19b8-129">이 예제에서 폼의 기본 HTML 구문은 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="c19b8-130">이 태그 페이지에서 실행 되 면 다음 그림이에서는 같은 간단한 폼을 만듭니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![브라우저에서 렌더링으로 HTML 양식 기본](form-basics/_static/image2.png)

<span data-ttu-id="c19b8-132">`<form>` 요소 전송할 HTML 요소를 포함 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="c19b8-133">(있도록 쉬운 실수를 페이지에 요소를 추가 하지만 안에 배치 해야 하는 것을 `<form>` 요소입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="c19b8-134">이런 경우, 아무 것도 제출 됩니다.) `method` 특성은 브라우저를 사용자 입력을 제출 하는 방법을 지시 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="c19b8-135">이 `post` 서버의 또는 업데이트를 수행 하는 경우 `get` 서버에서 데이터를 인출만 하는 경우.</span><span class="sxs-lookup"><span data-stu-id="c19b8-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="c19b8-136">**GET, POST 및 HTTP 동사 안전성**</span><span class="sxs-lookup"><span data-stu-id="c19b8-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="c19b8-137">HTTP, 브라우저 및 서버를 사용 하 여 정보를 교환 하는 프로토콜은 기본 작업에 매우 간단 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="c19b8-138">브라우저는 서버에 요청할만 몇 가지 동사를 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="c19b8-139">웹에 대 한 코드를 작성할 때 이러한 동사와 브라우저 및 서버를 사용 하는 방법 이해에 도움이 됩니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="c19b8-140">멀리 떨어진 가장 자주 사용 되는 동사는 다음과 같습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="c19b8-141">`GET`.</span><span class="sxs-lookup"><span data-stu-id="c19b8-141">`GET`.</span></span> <span data-ttu-id="c19b8-142">브라우저는 서버에서 항목을이 동사를 사용 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="c19b8-143">예를 들어 브라우저에 URL을 입력 하면 브라우저 수행을 `GET` 원하는 페이지를 요청 하는 작업입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="c19b8-144">브라우저 추가 수행 페이지 그래픽에 포함 된 경우 `GET` 이미지 작업 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="c19b8-145">경우는 `GET` 서버로 정보를 전달 해야 하는 작업, 정보를 쿼리 문자열에서 URL의 일부로 전달 됩니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="c19b8-146">`POST`.</span><span class="sxs-lookup"><span data-stu-id="c19b8-146">`POST`.</span></span> <span data-ttu-id="c19b8-147">브라우저에서 보내는 `POST` 추가 하거나 서버에서 변경 데이터를 제출 하기 위해 요청 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="c19b8-148">예를 들어를 `POST` 동사는 데이터베이스에서 레코드를 만들거나 기존 변경 하는 데 사용 됩니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="c19b8-149">대부분의 양식을 작성 및 제출 단추 클릭, 시간, 브라우저 수행을 `POST` 작업 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="c19b8-150">에 `POST` 작업 페이지의 본문에 서버로 전달 되는 데이터입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="c19b8-151">이러한 동사의 중요 한 차이를 `GET` 서버에 아무 것도 변경 하지 않는 작업-약간 더 추상적인 방식으로 전환할 수는 `GET` 작업 서버에서 상태가 변경 되지는지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="c19b8-152">수행할 수 있습니다는 `GET` 만큼 원하는 및 해당 리소스를 변경 하지 않습니다 동일한 리소스에 대 한 작업입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="c19b8-153">(A `GET` 작업은 말이 "안전한" 하거나 기술 용어를 사용 하는 *idempotent*.) 반대로, 물론는 `POST` 요청 변경 작업을 수행할 때마다 서버에서 작업 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="c19b8-154">두 예제에서는 이러한 차이 설명 하는 데 도움이 됩니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="c19b8-155">검색을 수행할 때 텍스트 상자 하나가 구성 된 양식에서 입력 하 고 검색 단추를 클릭 한 다음 Bing 이나 Google 같은 엔진을 사용 하 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="c19b8-156">수행 하는 브라우저는 `GET` URL의 일부분으로 전달 하 고 상자에 입력 한 값을 사용 하 여 작업 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="c19b8-157">사용 하는 `GET` 작업 정보 검색 작업을 서버에서 모든 리소스를 변경 하지 않으므로 이러한 유형의 폼 것에 인출 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="c19b8-158">이제 온라인 작업 주문 과정을 살펴보겠습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="c19b8-159">주문 세부 정보를 입력 하 고이 정보를 제출 단추를 클릭 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="c19b8-160">이 작업 수를 `POST` 작업이 가장 많은 다른 변경 내용을 새 주문 레코드를, 계정 정보 변경 등 서버에서 변경 하면 때문에 요청 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="c19b8-161">달리 합니다 `GET` 를 반복할 수 작업에 `POST` 요청-수행한 경우, 요청을 다시 전송 될 때마다 서버에서 새 주문을 생성 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="c19b8-162">(다음과 같은 경우에 웹 사이트에서 종종 경고 메시지가 전송 단추를 두 번 이상 클릭 필요가 또는 실수로 폼을 다시 제출 하지 있도록 제출 단추를 사용 하지 않도록 설정 됩니다.)</span><span class="sxs-lookup"><span data-stu-id="c19b8-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="c19b8-163">이 자습서에서는 과정에서 사용 하 여 모두를 `GET` 작업 및 `POST` HTML 양식을 사용 하는 작업입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="c19b8-164">각 사례 때문에 사용 되는 동사는 적절 한 설명입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="c19b8-165">(HTTP 동사에 대 한 자세한 내용은 참조는 [메서드 정의](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) W3C 사이트 문서.)</span><span class="sxs-lookup"><span data-stu-id="c19b8-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>


<span data-ttu-id="c19b8-166">대부분의 사용자 입력된 요소의 HTML은 `<input>` 요소입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="c19b8-167">와 같은 모양이 `<input type="type" name="name">,` 여기서 *형식* 원하는 사용자 입력된 컨트롤의 종류를 나타냅니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="c19b8-168">이러한 요소는 일반적으로:</span><span class="sxs-lookup"><span data-stu-id="c19b8-168">These elements are the common ones:</span></span>

- <span data-ttu-id="c19b8-169">텍스트 상자: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="c19b8-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="c19b8-170">확인란: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="c19b8-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="c19b8-171">라디오 단추: `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="c19b8-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="c19b8-172">단추: `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="c19b8-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="c19b8-173">제출 단추: `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="c19b8-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="c19b8-174">사용할 수도 있습니다는 `<textarea>` 요소를 여러 줄 텍스트 상자 만들기 및 `<select>` 드롭다운 목록에서 만들거나 스크롤 가능한 목록이 요소입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="c19b8-175">(HTML에 대 한 자세한 구성 요소를 참조 하세요 [HTML 양식 및 입력](http://www.w3schools.com/html/html_forms.asp) W3Schools 사이트의.)</span><span class="sxs-lookup"><span data-stu-id="c19b8-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="c19b8-176">`name` 이름이 나중에 요소 값을 어떻게 받게 됩니다 곧 알게 되었기 때문 특성은 매우 중요 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="c19b8-177">흥미로운 부분은, 페이지 개발자가 무엇을 할 사용자의 입력을 사용 하 여 보여 줍니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="c19b8-178">이러한 요소와 연결 된 기본 제공 동작은 없습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="c19b8-179">대신, 사용자 입력 또는 선택 된 값을 얻으려면 한 사람과 작업을 수행 해야 할 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="c19b8-180">이 자습서의 학습 내용입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="c19b8-181">**HTML5 및 입력된 폼**</span><span class="sxs-lookup"><span data-stu-id="c19b8-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="c19b8-182">아시다시피, HTML 전환 중에서 이며 최신 버전 (HTML5) 정보를 입력 하는 사용자에 대 한 보다 직관적인 방법에 대 한 지원이 포함 되어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="c19b8-183">예를 들어, html5로 (페이지 개발자) 할 수 있습니다 페이지 하려는 날짜를 입력 하는 사용자입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="c19b8-184">사용자는 날짜를 수동으로 입력 하도록 요구 하는 것이 아니라 일정 브라우저는 자동으로 표시한 다음 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="c19b8-185">그러나 HTML5는 새로운 클래스 이며 아직 모든 브라우저에서 지원 되지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="c19b8-186">ASP.NET 웹 페이지에 입력 하는 사용자의 브라우저는 HTML5 지원 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="c19b8-187">새 특성에 대 한 개념은 `<input>` html5로 요소를 참조 하세요 [HTML &lt;입력&gt; 특성을 입력](http://www.w3schools.com/html/html_form_input_types.asp) W3Schools 사이트입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>


## <a name="creating-the-form"></a><span data-ttu-id="c19b8-188">폼 만들기</span><span class="sxs-lookup"><span data-stu-id="c19b8-188">Creating the Form</span></span>

<span data-ttu-id="c19b8-189">WebMatrix에서에 **파일** 작업 영역을 열고 합니다 *Movies.cshtml* 페이지입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="c19b8-190">닫은 후 `</h1>` 태그와 여 `<div>` 태그를 `grid.GetHtml` 호출, 다음 태그를 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="c19b8-191">이 태그 라는 텍스트 상자가 포함 된 양식을 만듭니다 `searchGenre` 및 전송 단추가 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="c19b8-192">텍스트 상자 및 단추 묶인 제출를 `<form>` 요소입니다 `method` 특성이로 설정 된 `get`합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="c19b8-193">(텍스트 상자를 입력 하 고 제출 단추 안에 없는 경우를 `<form>` 요소에 아무 것도 제출 됩니다. 단추를 클릭 합니다.) 사용할는 `GET` 동사 여기 양식을 만들 수 있으므로 해당 작업을 수행할 변경 내용을 서버의-검색에만 발생 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="c19b8-194">(이전 자습서를 사용 하는 `post` 메서드를 서버에 변경 내용을 제출 하는 방법입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="c19b8-195">표시 됩니다는 다음 자습서에서 다시.)</span><span class="sxs-lookup"><span data-stu-id="c19b8-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="c19b8-196">페이지를 실행 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-196">Run the page.</span></span> <span data-ttu-id="c19b8-197">폼에 대 한 모든 동작을 정의 하지 않은 모양을 확인할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![장르에 대 한 검색 상자를 사용 하 여 동영상 페이지](form-basics/_static/image3.png)

<span data-ttu-id="c19b8-199">"코미디."와 같은 텍스트 상자에 값을 입력 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="c19b8-200">누른 **검색 장르**합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="c19b8-201">페이지의 URL을 기록해 둡니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-201">Take note of the URL of the page.</span></span> <span data-ttu-id="c19b8-202">설정 된 `<form>` 요소의 `method` 특성을 `get`, 입력 값이 같은 URL에 쿼리 문자열의 일부가 되었습니다.:</span><span class="sxs-lookup"><span data-stu-id="c19b8-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="c19b8-203">읽기 폼 값</span><span class="sxs-lookup"><span data-stu-id="c19b8-203">Reading Form Values</span></span>

<span data-ttu-id="c19b8-204">페이지는 이미 데이터베이스 데이터를 가져오고 결과 표로 표시 하는 일부 코드를 포함 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="c19b8-205">이제 검색 용어를 포함 하는 SQL 쿼리를 실행할 수 있도록 입력란의 값을 읽는 코드를 추가 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="c19b8-206">폼의 메서드 설정 했으므로 `get`, 다음과 같은 코드를 사용 하 여 텍스트 상자에 입력 된 값을 읽을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="c19b8-207">`Request.QueryString` 개체 (를 `QueryString` 의 속성을 `Request` 개체)의 일부로 전송 된 요소의 값이 포함 됩니다는 `GET` 작업.</span><span class="sxs-lookup"><span data-stu-id="c19b8-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="c19b8-208">`Request.QueryString` 속성을 포함를 *컬렉션* (목록)의 형태로 전송 되는 값입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="c19b8-209">모든 개별 값을 가져오려면 원하는 요소의 이름을 지정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="c19b8-210">이유는 있어야를 `name` 특성을 `<input>` 요소 (`searchTerm`) 텍스트 상자를 만드는 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="c19b8-211">(대 한 자세한 내용은 합니다 `Request` 개체를 참조 하십시오 합니다 [사이드바](#BKMK_TheRequestObject) 나중.)</span><span class="sxs-lookup"><span data-stu-id="c19b8-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="c19b8-212">간단 충분히 입력란의 값을 읽을 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="c19b8-213">사용자 입력 하지 않았습니다 아무 것도 전혀 텍스트 상자에 있지만 클릭 하지만 **검색** 어쨌든 검색할 항목이 없으므로 해당 클릭 무시할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="c19b8-214">다음 코드는 이러한 조건을 구현 하는 방법을 보여 주는 예제입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="c19b8-215">(아직이 코드를 추가할 필요가 없습니다; 잠시 후에 로그인 할 수 있습니다)입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="c19b8-216">테스트는 이러한 방식으로 나눕니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="c19b8-217">값을 가져옵니다 `Request.QueryString["searchGenre"]`, 즉에 입력 된 값을 `<input>` 라는 요소가 `searchGenre`합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="c19b8-218">비어 있는지를 사용 하 여 확인 된 `IsEmpty` 메서드.</span><span class="sxs-lookup"><span data-stu-id="c19b8-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="c19b8-219">이 메서드는 표준 방법 항목 (예를 들어, 폼 요소)는 값이 들어 있는지 확인 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="c19b8-220">하지만 실제로 관심이 있는 경우에 *되지* 비어 있으므로...</span><span class="sxs-lookup"><span data-stu-id="c19b8-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="c19b8-221">추가 된 `!` 앞의 연산자는 `IsEmpty` 테스트 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="c19b8-222">(의 `!` 연산자 의미 논리적 NOT).</span><span class="sxs-lookup"><span data-stu-id="c19b8-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="c19b8-223">전체 일반 영어로 `if` 조건이 다음 변환: *양식의 searchGenre 요소 비어 있지 않은 경우, 다음 중...*</span><span class="sxs-lookup"><span data-stu-id="c19b8-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="c19b8-224">이 블록에는 검색 용어를 사용 하는 쿼리를 만들기 위한 단계를 설정 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="c19b8-225">다음 섹션에서 할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="c19b8-226">**요청 개체**</span><span class="sxs-lookup"><span data-stu-id="c19b8-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="c19b8-227">`Request` 개체 브라우저 페이지를 요청 하거나 제출 하는 경우 응용 프로그램에 전송 하는 모든 정보를 포함 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="c19b8-228">이 개체에 텍스트 상자 값 또는 업로드할 파일을 같은 사용자 제공 하는 모든 정보가 포함 됩니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="c19b8-229">또한 모든 종류의 쿠키 (있는 경우) URL 쿼리 문자열의 값과 같은 추가 정보를 실행 중인 브라우저에 설정 된 언어 목록에 사용자를 사용 하는 브라우저의 종류는 페이지의 파일 경로 를 확인해 보십시오.</span><span class="sxs-lookup"><span data-stu-id="c19b8-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="c19b8-230">`Request` 개체를 *컬렉션* (목록) 값입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="c19b8-231">해당 이름을 지정 하 여 컬렉션에서 개별 값을 가져옵니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="c19b8-232">`Request` 개체는 실제로 여러 하위 집합을 노출 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="c19b8-233">예를 들어:</span><span class="sxs-lookup"><span data-stu-id="c19b8-233">For example:</span></span>
> 
> - <span data-ttu-id="c19b8-234">`Request.Form` 제출 된 내부 요소에서 값을 제공 `<form>` 요청인 경우 요소는 `POST` 요청 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="c19b8-235">`Request.QueryString` 제공 된 값만 URL의 쿼리 문자열에 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="c19b8-236">(같은 URL에서 `http://mysite/myapp/page?searchGenre=action&page=2`, `?searchGenre=action&page=2` URL의 섹션은 쿼리 문자열.)</span><span class="sxs-lookup"><span data-stu-id="c19b8-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="c19b8-237">`Request.Cookies` 컬렉션은 브라우저에 보내지는 쿠키에 액세스할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="c19b8-238">제출된 된 폼에는 사용자가 알고 있는 값을 가져오는 사용할 수 있습니다 `Request["name"]`합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="c19b8-239">또는 특정 버전을 사용할 수 `Request.Form["name"]` (에 대 한 `POST` 요청) 또는 `Request.QueryString["name"]` (에 대 한 `GET` 요청).</span><span class="sxs-lookup"><span data-stu-id="c19b8-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="c19b8-240">물론 *이름을* 가져올 항목의 이름입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="c19b8-241">가져오려는 항목의 이름을 사용 하는 컬렉션 내에서 고유 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="c19b8-242">때문에 합니다 `Request` 개체와 같은 하위 집합을 제공 `Request.Form` 고 `Request.QueryString`합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="c19b8-243">페이지 라는 폼 요소를 포함 한다고 가정 `userName` 하 고 *수도* 이름이 포함 `userName`합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="c19b8-244">표시 되 면 `Request["userName"]`, 모호 양식 값 또는 쿠키 것인지 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="c19b8-245">그러나 받게 되 면 `Request.Form["userName"]` 또는 `Request.Cookie["userName"]`를 가져오려고 하는 값에 대 한 명시적 예요.</span><span class="sxs-lookup"><span data-stu-id="c19b8-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="c19b8-246">특정 수의 하위 집합을 사용 하는 것이 좋습니다 `Request` 원하는 같은 있다고 `Request.Form` 또는 `Request.QueryString`합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="c19b8-247">이 자습서에서 만드는 간단한 페이지, 아마도 하지 않는 실제로 것 간 차이입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="c19b8-248">그러나 더 복잡 한 페이지를 만들 때 버전을 사용 하는 명시적 `Request.Form` 또는 `Request.QueryString` 페이지 폼 (또는 여러 개의 폼)을 포함 하는 경우 발생할 수 있는 문제를 방지 하는 데 도움이 쿠키, 쿼리 문자열 값 및 등입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>


## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="c19b8-249">검색 용어를 사용 하 여 쿼리 만들기</span><span class="sxs-lookup"><span data-stu-id="c19b8-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="c19b8-250">사용자가 입력 한 검색어를 가져오는 방법을 배웠으므로를 사용 하는 쿼리를 만들 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="c19b8-251">데이터베이스에서 모든 동영상 항목을 가져오려면 사용 하는 경우이 문은 같은 SQL 쿼리를 기억해 야 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="c19b8-252">포함 하는 쿼리를 사용 해야만 특정 영화를 가져오려면는 `Where` 절.</span><span class="sxs-lookup"><span data-stu-id="c19b8-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="c19b8-253">이 절을 쿼리에 의해 반환 된 행에는 조건을 설정할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="c19b8-254">예를 들면 다음과 같습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="c19b8-255">기본 형식은 `WHERE column = value`합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="c19b8-256">방금 외에 다른 연산자를 사용할 수 있습니다 `=`과 같이 `>` (보다 큼), `<` (보다 작음), `<>` (같지 않음), `<=` (작거나 같음), 원하는 항목에 따라 등.</span><span class="sxs-lookup"><span data-stu-id="c19b8-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="c19b8-257">SQL 문은 궁금한 경우 대/소문자 구분 하지 않습니다 &mdash; `SELECT` 와 같습니다 `Select` (심지어 `select`).</span><span class="sxs-lookup"><span data-stu-id="c19b8-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="c19b8-258">그러나 사람들이 자주 활용 SQL 문에서 키워드와 같은 `SELECT` 고 `WHERE`, 쉽게 읽을 수 있도록 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="c19b8-259">검색 용어를 매개 변수로 전달</span><span class="sxs-lookup"><span data-stu-id="c19b8-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="c19b8-260">특정 장르를 아주 쉽게 검색 (`WHERE Genre = 'Action'`), 하지만 사용자가 입력 하는 모든 장르를 검색할 수 있게 되기를 원하는 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="c19b8-261">이 작업을 수행 하려면 검색 하는 값에 대 한 자리 표시자를 포함 하는 SQL 쿼리를 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="c19b8-262">이 명령은 같습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="c19b8-263">자리 표시자는는 `@` 문자 뒤에 0 개 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="c19b8-264">쿼리는 여러 자리 표시자를 포함할 수 있습니다 및 명명 된은 짐작할 수 `@0`, `@1`, `@2`등입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="c19b8-265">쿼리를 설정 하 고 실제로 값을 전달 하려면 다음과 같은 코드를 사용할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="c19b8-266">이 코드는 이미 작업 표에서 데이터를 표시할 비슷합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="c19b8-267">유일한 차이점은:</span><span class="sxs-lookup"><span data-stu-id="c19b8-267">The only differences are:</span></span>

- <span data-ttu-id="c19b8-268">자리 표시자를 포함 하는 쿼리 (`WHERE Genre = @0"`).</span><span class="sxs-lookup"><span data-stu-id="c19b8-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="c19b8-269">쿼리 변수에 배치 됩니다 (`selectCommand`)에 직접 쿼리를 전달 하기 전에;는 `db.Query` 메서드.</span><span class="sxs-lookup"><span data-stu-id="c19b8-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="c19b8-270">호출 하는 경우는 `db.Query` 메서드를 전달 하면 쿼리 및 자리 표시자에 사용할 값입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="c19b8-271">(전달는 쿼리가 여러 자리 표시자 있으면 메서드를 별도 값으로 모든.)</span><span class="sxs-lookup"><span data-stu-id="c19b8-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="c19b8-272">이러한 모든 요소를 결합 하는 경우 다음 코드를 가져옵니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="c19b8-273">**중요!**</span><span class="sxs-lookup"><span data-stu-id="c19b8-273">**Important!**</span></span> <span data-ttu-id="c19b8-274">자리 표시자를 사용 하 여 (같은 `@0`) 값을 SQL 명령에 전달 하는 *매우 중요 한* 보안에 대 한 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="c19b8-275">변수 데이터에 대 한 자리 표시자를 사용 하 여 여기에서 살펴본 방식에는 유일한 방법은 SQL 명령을 생성 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="c19b8-276">리터럴 텍스트 (연결) 및 사용자에서 가져온 값을 결합 하 여 SQL 문을 생성 하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="c19b8-277">사이트를 열고 SQL 문으로 사용자 입력을 연결 하는 *SQL 주입 공격* 악의적인 사용자 데이터베이스를 해킹 하는 페이지에 대 한 값을 제출 하는 경우.</span><span class="sxs-lookup"><span data-stu-id="c19b8-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="c19b8-278">(자세한 내용은 문서의 [SQL 주입](https://msdn.microsoft.com/library/ms161953.aspx) MSDN 웹 사이트.)</span><span class="sxs-lookup"><span data-stu-id="c19b8-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>


## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="c19b8-279">코드 검색을 사용 하 여 영화 페이지 업데이트</span><span class="sxs-lookup"><span data-stu-id="c19b8-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="c19b8-280">이제에서 코드를 업데이트 하는 *Movies.cshtml* 파일입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="c19b8-281">시작 하려면 페이지의 맨 위에 있는 코드 블록의 코드를이 코드로 바꿉니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="c19b8-282">여기서 차이점은 쿼리를 배치한 합니다 `selectCommand` 에 전달 하는 변수 `db.Query` 나중입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="c19b8-283">SQL 문을 변수에 배치는 문을 검색 하는 데 수행할 수를 변경할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="c19b8-284">또한 나중에 다시 배치에서는이 두 줄을 제거 했습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="c19b8-285">쿼리를 아직 실행 하지 않으려는 (즉, 호출 `db.Query`)를 초기화 하지는 `WebGrid` 도우미 아직 중 하나입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="c19b8-286">SQL 문을 실행 해야 파악 한 후 이러한 작업을 수행할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="c19b8-287">다시 작성된이 블록 뒤에 검색 처리에 대 한 새 논리를 추가할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="c19b8-288">완성 된 코드는 다음과 같이 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-288">The completed code will look like the following.</span></span> <span data-ttu-id="c19b8-289">이 예제에서는 일치 하도록 페이지에서 코드를 업데이트 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="c19b8-290">페이지는 이제 다음과 같이 작동합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-290">The page now works like this.</span></span> <span data-ttu-id="c19b8-291">페이지 실행 될 때마다 코드가 데이터베이스 및 `selectCommand` 변수에서 모든 레코드를 가져오는 SQL 문을로 설정 됩니다는 `Movies` 테이블입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="c19b8-292">초기화 코드는 `searchTerm` 변수입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="c19b8-293">그러나 현재 요청에 대 한 값을 포함 하는 경우는 `searchGenre` 요소를 코드 집합 `selectCommand` 다른 쿼리를-하는 namely를 포함 하는 `Where` 장르를 검색 하는 절.</span><span class="sxs-lookup"><span data-stu-id="c19b8-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="c19b8-294">또한 설정 `searchTerm` (nothing 일 수 있음)는 검색 상자에 전달 되는 항목에 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="c19b8-295">문을는 SQL에 관계 없이 `selectCommand`, 호출 `db.Query` 쿼리를 실행 하려면 전달 어떤 중인 `searchTerm`합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="c19b8-296">내용이 없는 경우 `searchTerm`, 중요 하지 않습니다, 경우에 값을 전달 하려면 매개 변수가 있기 때문에 `selectCommand` 진행 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="c19b8-297">마지막으로 코드를 초기화 합니다 `WebGrid` 이전 처럼 쿼리 결과 사용 하 여 도우미입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="c19b8-298">SQL 문을 배치 하 여 볼 수 있습니다 및 변수로 검색어 코드에 유연성이 추가 했습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="c19b8-299">이 자습서의 뒷부분에서 살펴보겠지만이 필요한 기본적인 프레임 워크를 사용할 수 하 고 다양 한 검색에 대 한 논리를 계속 추가 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="c19b8-300">장르 하 여 검색 기능을 테스트</span><span class="sxs-lookup"><span data-stu-id="c19b8-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="c19b8-301">WebMatrix를 실행 합니다 *Movies.cshtml* 페이지입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="c19b8-302">장르에 대 한 텍스트 상자를 사용 하 여 페이지가 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="c19b8-303">사용자 테스트 레코드 중 하나에 대해 입력 한 다음 클릭는 장르를 입력 **검색**합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="c19b8-304">이 이번에만 해당 장르와 일치 하는 영화 목록을 표시 됩니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-304">This time you see a listing of just the movies that match that genre:</span></span>

![Comedies' genre'에 대 한 검색 후 나열 동영상 페이지](form-basics/_static/image4.png)

<span data-ttu-id="c19b8-306">다른 장르를 입력 하 고 다시 검색 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-306">Enter a different genre and search again.</span></span> <span data-ttu-id="c19b8-307">검색은 대/소문자 구분을 볼 수 있도록 모든 소문자 또는 대문자로 문자를 사용 하 여 장르를 입력 하십시오.</span><span class="sxs-lookup"><span data-stu-id="c19b8-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="c19b8-308">사용자가 입력 한 내용이 "기억"</span><span class="sxs-lookup"><span data-stu-id="c19b8-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="c19b8-309">보시는 장르를 입력 하 고 클릭 한 후 **검색 장르**, 해당 장르에 대 한 목록을 확인 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="c19b8-310">검색 텍스트 상자 비어 있지만 &mdash; 이 즉,에 입력 했다고 기억 하지 않았습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="c19b8-311">이 동작이 발생 하는 이유를 이해 하는 것이 반드시 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="c19b8-312">페이지에 제출 하면 브라우저는 웹 서버에 요청을 보냅니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="c19b8-313">ASP.NET 요청 가져오면이 페이지의 새 인스턴스를 만듭니다, 그리고 코드를 실행 및 브라우저 페이지를 다시 렌더링 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="c19b8-314">실제로 그러나 페이지 알지는 작업 중이 던 이전 버전의 자체를 사용 하 여 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="c19b8-315">일부 요청도 알고 모두에 데이터를 형성 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="c19b8-316">페이지를 요청 될 때마다 &mdash; 처음 아니면 모르니 까 &mdash; 새 페이지 보시기 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="c19b8-317">웹 서버에 마지막 요청 메모리가 없습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="c19b8-318">모두 ASP.NET 않으며 브라우저도 않습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="c19b8-319">페이지의 이러한 별도 인스턴스 간의 유일한 연결이 서로 전송 하는 모든 데이터입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="c19b8-320">페이지에 제출 하면 예를 들어, 새 페이지 인스턴스 데이터를 가져올 수는 폼 이전 인스턴스에서 보낸 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="c19b8-321">(페이지 간에 데이터를 전달 하는 또 다른 방법은 쿠키를 사용 합니다.)</span><span class="sxs-lookup"><span data-stu-id="c19b8-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="c19b8-322">이 상황을 설명 하는 정식 방법을 웹 페이지는 싶습니다 *상태 비저장*합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="c19b8-323">웹 서버와 페이지 자체 및 페이지의 요소에는 페이지의 이전 상태에 대 한 정보를 유지 하지 않습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="c19b8-324">웹 개별 요청에 대 한 상태를 유지 관리 신속 하 게 소모 하 게 종종 아마도 수천을 처리 하는 웹 서버의 리소스가 수십만, 초당 요청 때문에 이러한 방식으로 설계 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="c19b8-325">그렇기 때문 텍스트 상자가 비어 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-325">So that's why the text box was empty.</span></span> <span data-ttu-id="c19b8-326">페이지를 제출한 후 ASP.NET 페이지의 새 인스턴스를 생성 하 고 코드와 태그를 통해 실행 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="c19b8-327">텍스트 상자에 값을 배치 하기 위해 ASP.NET에 지시 하는 해당 코드에서 nothing 있었습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="c19b8-328">따라서 ASP.NET 하지 않은 모든 작업을 수행할 및 텍스트 상자에 값이 없는 렌더링 되었습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="c19b8-329">실제로이 문제를 해결 하는 쉬운 방법이 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="c19b8-330">텍스트 상자에 입력 한 장르 *됩니다* 코드에서 사용할 수 있습니다 &mdash; 있는 `Request.QueryString["searchGenre"]`합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="c19b8-331">텍스트 상자에 대 한 태그를 업데이트 하는 `value` 특성에서 값을 가져옵니다 `searchTerm`,이 예제와 같이:</span><span class="sxs-lookup"><span data-stu-id="c19b8-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="c19b8-332">이 페이지에서 설정할 수 있습니다도 합니다 `value` 특성을 `searchTerm` 입력 변수를 해당 변수에 장르를 포함 하기 때문입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="c19b8-333">하지만 사용 하 여는 `Request` 설정 하는 개체를 `value` 같습니다.이 작업을 수행 하는 표준 방법은 표시 된 것 처럼 특성입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="c19b8-334">(이 작업을 수행 하려는 것으로 가정 &mdash; 상황에 따라 페이지를 렌더링 하려는 *없이* 필드의 값입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="c19b8-335">모든 종속 응용 프로그램을 사용 하 여 것입니다.)</span><span class="sxs-lookup"><span data-stu-id="c19b8-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="c19b8-336">기억할 수 없는 "" 암호에 사용 되는 입력란의 값입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="c19b8-337">사람이 코드를 사용 하 여 암호 필드를 채울 수 있도록 보안 허점을 것입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>


<span data-ttu-id="c19b8-338">페이지를 다시 실행을 장르를 입력 한 다음 클릭 **검색 장르**합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="c19b8-339">이 시간 뿐만 아니라 있습니다 볼 검색 결과 수 있지만 입력란 기억 마지막 입력:</span><span class="sxs-lookup"><span data-stu-id="c19b8-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![페이지를 보여 주는 텍스트 상자에 ' 저장 ' 이전 항목](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="c19b8-341">제목에 단어 검색</span><span class="sxs-lookup"><span data-stu-id="c19b8-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="c19b8-342">이제 모든 장르를 검색할 수 있습니다 하지만 제목을 검색할 수도 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="c19b8-343">까다로웠습니다. 제목을 정확 하 게 검색 하는 경우, 대신 제목 내의 아무 곳 이나 표시 되는 단어를 검색할 수 있습니다 하는 것이 어렵습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="c19b8-344">SQL에서 이렇게 하려면 사용는 `LIKE` 연산자 및 구문을 다음과 같이 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="c19b8-345">이 명령은 모든 영화를 제목에 해당 어구가 포함 "adventure"를 가져옵니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="c19b8-346">사용 하는 경우는 `LIKE` 와일드 카드 문자를 포함 하면 연산자 `%` 검색어의 일환으로 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="c19b8-347">검색 `LIKE 'adventure%'` "'a d v를 사용 하 여 시작"을 의미 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="c19b8-348">(기술적으로 "문자열 뒤에 ' adventure'입니다." 의미) 검색어 마찬가지로 `LIKE '%adventure'` "모든 항목을 의미 'a d v 문자열 뒤에", "a d v e'로 끝나는"를 다른 방법으로 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="c19b8-349">검색어 `LIKE '%adventure%'` "a d v 제목에 있습니다." 사용 "을 의미 하므로</span><span class="sxs-lookup"><span data-stu-id="c19b8-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="c19b8-350">(기술적으로 "모든 항목에 제목 뒤에 'adventure' 뒤에.")</span><span class="sxs-lookup"><span data-stu-id="c19b8-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="c19b8-351">내 합니다 `<form>` 요소에 닫는 바로 아래에서 다음 태그를 추가 `</div>` 장르 검색에 대 한 태그 (닫기 전에 `</form>` 요소):</span><span class="sxs-lookup"><span data-stu-id="c19b8-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="c19b8-352">이 검색을 처리 하는 코드는 조립 해야 한다는 장르 검색에 대 한 코드와 유사 합니다 `LIKE` 검색 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="c19b8-353">페이지의 맨 위에 있는 코드 블록 내에서이 추가 `if` 바로 뒤에 블록을 `if` 장르 검색에 대 한 블록:</span><span class="sxs-lookup"><span data-stu-id="c19b8-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="c19b8-354">이 코드는 이전에 본 것과 동일한 논리 제외 하 고 검색을 사용 하 여는 `LIKE` 연산자와 코드 넣습니다 "`%`" 검색어 전후.</span><span class="sxs-lookup"><span data-stu-id="c19b8-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="c19b8-355">쉽게 페이지 검색을 다시 추가 된 방식을 확인할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="c19b8-356">수행 했던 모든 했습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-356">All you had to do was:</span></span>

- <span data-ttu-id="c19b8-357">만들기는 `if` 관련 검색 상자는 값을가지고 있는지 여부를 확인 하려면 테스트 하는 블록입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="c19b8-358">설정 된 `selectCommand` 새 SQL 문에 변수입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="c19b8-359">설정 된 `searchTerm` 쿼리에 전달할 값으로 변수입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="c19b8-360">제목 검색에 대 한 새 논리를 포함 하는 전체 코드 블록을 다음과 같습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="c19b8-361">요약의이 코드의 용도 다음과 같습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="c19b8-362">변수 `searchTerm` 고 `selectCommand` 맨 위에 있는 초기화 됩니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="c19b8-363">이러한 변수를 적절 한 검색 용어 (있는 경우)으로 설정 하려는 및 적절 한 SQL 명령에서 사용자가 페이지에 기반 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="c19b8-364">기본 검색 데이터베이스에서 모든 영화를 가져오는 간단한 경우입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="c19b8-365">에 대 한 테스트 `searchGenre` 및 `searchTitle`, 코드 집합 `searchTerm` 검색 하려는 값입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="c19b8-366">해당 코드 블록을 설정할 수도 `selectCommand` 해당 검색에 대 한 적절 한 SQL 명령에 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="c19b8-367">합니다 `db.Query` 메서드를 한 번만 사용 하 여 모든 SQL 명령에 `selectedCommand` 값 이며 `searchTerm`합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="c19b8-368">(없음 장르 및 없습니다 제목 단어) 검색 용어가 없는 경우 값 `searchTerm` 은 빈 문자열입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="c19b8-369">그러나 문제가 되지 경우 쿼리 매개 변수를 필요 하지 않으므로 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="c19b8-370">제목 검색 기능 테스트</span><span class="sxs-lookup"><span data-stu-id="c19b8-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="c19b8-371">이제 완성 된 검색 페이지를 테스트할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-371">Now you can test your completed search page.</span></span> <span data-ttu-id="c19b8-372">실행할 *Movies.cshtml*합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="c19b8-373">장르를 입력 하 고 클릭 **검색 장르**합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="c19b8-374">표에서 전에 등의 해당 장르의 영화를 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="c19b8-375">제목 단어를 입력 하 고 클릭 **검색 제목을**입니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="c19b8-376">표 제목에 해당 단어가 포함 된 영화를 표시 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-376">The grid displays movies that have that word in the title.</span></span>

![동영상 페이지 제목에 '는'에 대 한 검색 후 나열](form-basics/_static/image6.png)

<span data-ttu-id="c19b8-378">두 텍스트 상자를 비워 두고 단추 중 하나를 클릭 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="c19b8-379">표의 모든 영화를 표시합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="c19b8-380">쿼리를 결합합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-380">Combining the Queries</span></span>

<span data-ttu-id="c19b8-381">전용 검색을 수행할 수 있습니다는 확인할 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="c19b8-382">두 검색 상자에 값을 포함 하는 경우에 제목과 장르를 동시에 검색할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="c19b8-383">예를 들어, 해당 제목 "Adventure"를 포함 하는 모든 작업 영화를 검색할 수 없습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="c19b8-384">(페이지는 코딩 된 대로 이제 장르와 제목에 대 한 값을 입력 하면 제목 검색이 가져옵니다 우선 순위를입니다.) 조건을 결합 하는 검색을 만들려면 다음과 같은 구문이 SQL 쿼리를 만들 해야 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="c19b8-385">다음과 같은 문을 사용 하 여 쿼리를 실행 해야 하 고 (약 말하기):</span><span class="sxs-lookup"><span data-stu-id="c19b8-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="c19b8-386">알 수 있듯이 조건과 순열이 많이 수 있도록 논리를 만드는 약간 관련 된 가져올 수 있습니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="c19b8-387">따라서 여기서 중지 됩니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="c19b8-388">다음에 나오는</span><span class="sxs-lookup"><span data-stu-id="c19b8-388">Coming Up Next</span></span>

<span data-ttu-id="c19b8-389">다음 자습서에서는 사용자가 영화 데이터베이스에 추가할 수 있도록 폼을 사용 하는 페이지를 만들어야 합니다.</span><span class="sxs-lookup"><span data-stu-id="c19b8-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="c19b8-390">동영상 페이지 (검색을 사용 하 여 업데이트)에 대 한 전체 목록</span><span class="sxs-lookup"><span data-stu-id="c19b8-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="c19b8-391">추가 리소스</span><span class="sxs-lookup"><span data-stu-id="c19b8-391">Additional Resources</span></span>

- [<span data-ttu-id="c19b8-392">Razor 구문을 사용 하 여 ASP.NET 웹 프로그래밍 소개</span><span class="sxs-lookup"><span data-stu-id="c19b8-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="c19b8-393">[SQL WHERE 절](http://www.w3schools.com/sql/sql_where.asp) W3Schools 사이트</span><span class="sxs-lookup"><span data-stu-id="c19b8-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="c19b8-394">[메서드 정의](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) W3C 사이트에 대 한 문서</span><span class="sxs-lookup"><span data-stu-id="c19b8-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="c19b8-395">[이전](displaying-data.md)
> [다음](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="c19b8-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
